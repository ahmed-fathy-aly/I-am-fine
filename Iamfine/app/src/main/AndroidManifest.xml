<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.enterprises.wayne.iamfine">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name=".app.MyApplication">

        <activity android:name=".screen.sign_in.SignInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".screen.sign_up.SignUpActivity"/>

        <activity android:name=".screen.main_screen.MainScreenActivity">
        </activity>

        <provider
            android:name=".database.WhoAskedProvider"
            android:authorities="com.enterprises.wayne.i_am_fine"/>
    </application>

</manifest>